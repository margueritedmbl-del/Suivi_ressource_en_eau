"use client";import{useState}from"react";import{useRole}from"@/components/auth/useRole";
export default function ReportsPanel(){const{role,canOperate}=useRole();const[module,setModule]=useState("points_eau"),[periode,setPeriode]=useState("mensuel");return <div className="panel"><h2>Rapports techniques détaillés</h2><p><span className="role-badge">Rôle : {role}</span></p><p>Chaque export contient une synthèse et le détail des enregistrements disponibles pour le module et la période choisis.</p>{!canOperate&&<div className="notice-empty">Rapports et exports réservés à Administrateur PTCS et DNH/DRHK.</div>}<div className="grid-2"><select className="input" value={module} onChange={e=>setModule(e.target.value)}><option value="points_eau">Points d'eau</option><option value="pluviometrie">Pluviométrie</option><option value="piezometrie">Piézométrie</option><option value="limnimetrie">Limnimétrie</option></select><select className="input" value={periode} onChange={e=>setPeriode(e.target.value)}><option value="mensuel">Mois en cours</option><option value="trimestriel">Trimestre en cours</option><option value="annuel">Année en cours</option><option value="personnalisee">Toutes les données opérationnelles</option></select></div><div className="report-grid" style={{marginTop:16}}>{["pdf","docx","xlsx","csv"].map(f=><a key={f} className={canOperate?"btn btn-primary":"btn btn-primary disabled-action"} href={canOperate?`/api/reports/export?module=${module}&format=${f}&periode=${periode}`:"#"} target="_blank">Exporter {f.toUpperCase()}</a>)}</div></div>}

"use client";
import{useMemo,useState}from"react";import{useRole}from"@/components/auth/useRole";import{authFetch}from"@/lib/auth-client";
export default function ReportsPanel(){const{role,canAccessReports}=useRole();const[module,setModule]=useState("pluviometrie"),[periode,setPeriode]=useState("mensuel"),[start,setStart]=useState(""),[end,setEnd]=useState(""),[sb,setSb]=useState("98"),[busy,setBusy]=useState(""),[msg,setMsg]=useState("");const query=useMemo(()=>{const p=new URLSearchParams({module,periode});if(periode==="personnalisee"){if(start)p.set("start",start);if(end)p.set("end",end)}if(module==="sous_bassin"&&sb)p.set("sb",sb);return p.toString()},[module,periode,start,end,sb]);async function download(f:string){if(!canAccessReports)return;setBusy(f);setMsg("");try{const r=await authFetch(`/api/reports/export?${query}&format=${f}`);if(!r.ok){const j=await r.json().catch(()=>({}));throw new Error(j.error||`HTTP ${r.status}`)}const b=await r.blob(),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;const cd=r.headers.get("content-disposition")||"";const m=cd.match(/filename="?([^";]+)"?/i);a.download=m?.[1]||`psore_rapport.${f}`;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}catch(e:any){setMsg(`Erreur : ${e?.message||"export impossible"}`)}finally{setBusy("")}}
 return <div className="panel"><h2>Rapports techniques prêts pour présentation</h2><p><span className="role-badge">Rôle : {role}</span></p><p>Standard PSORE V5.0 : A4 paysage, synthèse, contrôles qualité, graphiques, carte, analyse factuelle et observations détaillées. Les valeurs absentes restent « Non renseigné » et aucune observation n'est supprimée automatiquement.</p>{!canAccessReports&&<div className="notice-empty">Rapports techniques réservés aux rôles DNH/DRHK, Administrateur PTCS et Super administrateur.</div>}<div className="grid-2"><select className="input" value={module} onChange={e=>setModule(e.target.value)}><option value="pluviometrie">Pluviométrie</option><option value="piezometrie">Piézométrie</option><option value="limnimetrie">Limnimétrie</option><option value="points_eau">Points d'eau</option><option value="sous_bassin">Rapport intégré de sous-bassin</option></select><select className="input" value={periode} onChange={e=>setPeriode(e.target.value)} disabled={module==="sous_bassin"}><option value="mensuel">Mois en cours</option><option value="trimestriel">Trimestre en cours</option><option value="annuel">Année en cours</option><option value="personnalisee">Période personnalisée / toutes les données</option></select></div>{module==="sous_bassin"&&<label style={{display:"block",marginTop:12}}><span>Identifiant du sous-bassin (ex. 98 pour SB-98)</span><input className="input" value={sb} onChange={e=>setSb(e.target.value.replace(/[^0-9]/g,""))}/></label>}{periode==="personnalisee"&&module!=="sous_bassin"&&<div className="grid-2" style={{marginTop:12}}><label><span>Début</span><input className="input" type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label><span>Fin</span><input className="input" type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label></div>}<div className="report-grid" style={{marginTop:16}}>{["pdf","docx","xlsx","csv"].map(f=><button key={f} className="btn btn-primary" disabled={!canAccessReports||!!busy} onClick={()=>download(f)}>{busy===f?"Génération...":`Exporter ${f.toUpperCase()}`}</button>)}</div>{msg&&<div className="notice-empty">{msg}</div>}</div>}

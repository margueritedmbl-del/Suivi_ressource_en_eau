export const dynamic="force-dynamic";export const revalidate=0;export const fetchCache="force-no-store";
import{NextRequest,NextResponse}from"next/server";import{supabaseAdmin}from"@/lib/supabase-admin";import{Document,Packer,Paragraph,HeadingLevel,Table,TableRow,TableCell,WidthType}from"docx";import*as XLSX from"xlsx";import{jsPDF}from"jspdf";
const CFG:any={pluviometrie:{table:"v_pluviometrie_dashboard_v47",date:"date_observation",cols:[["code_site","Station"],["nom_site","Localité"],["commune","Commune"],["date_observation","Date"],["pluie_24h_mm","Pluie 24 h (mm)"],["cumul_mensuel_mm","Cumul mensuel (mm)"],["observateur","Observateur"],["commentaire","Commentaire"]]},piezometrie:{table:"v_piezometrie_dashboard_v47",date:"date_observation",cols:[["code_site","Piézomètre"],["nom_site","Localité"],["commune","Commune"],["date_observation","Date"],["niveau_statique","Niveau statique (m)"],["observateur","Observateur"],["commentaire","Commentaire"]]},limnimetrie:{table:"v_limnimetrie_dashboard_v47",date:"date_observation",cols:[["code_site","Station"],["nom_site","Localité / cours d'eau"],["commune","Commune"],["date_observation","Date"],["periode","Période"],["hauteur_eau","Hauteur d'eau"],["observateur","Observateur"],["commentaire","Commentaire"]]},points_eau:{table:"points_eau",date:"synced_at",cols:[["code_pe","Code"],["village","Village"],["commune","Commune"],["type_ouvrage","Type"],["etat","État"],["fonctionnalite_forage","Fonctionnalité"],["equipement_forage","Équipement"],["organe_gestion","Gestion"],["ph","pH"],["besoin_rehabilitation","Besoin réhabilitation"]]}};
function periodStart(p:string){const d=new Date(),y=d.getUTCFullYear(),m=d.getUTCMonth();if(p==="mensuel")return new Date(Date.UTC(y,m,1)).toISOString();if(p==="trimestriel")return new Date(Date.UTC(y,Math.floor(m/3)*3,1)).toISOString();if(p==="annuel")return new Date(Date.UTC(y,0,1)).toISOString();return null}
function clean(rows:any[],cols:any[]){return rows.map(r=>Object.fromEntries(cols.map(([k,l]:any)=>[l,r?.[k]??""])))}function esc(v:any){return`"${String(v??"").replaceAll('"','""')}"`}function csv(rows:any[]){if(!rows.length)return"Aucune donnée\n";const h=Object.keys(rows[0]);return[h.map(esc).join(";"),...rows.map(r=>h.map(k=>esc(r[k])).join(";"))].join("\n")}
function summary(rows:any[],module:string){const codes=new Set(rows.map(r=>r.code_site||r.code_pe).filter(Boolean));const vals=rows.map(r=>Number(r.pluie_24h_mm??r.niveau_statique??r.hauteur_eau)).filter(Number.isFinite);return{enregistrements:rows.length,stations:codes.size,moyenne:vals.length?vals.reduce((a,b)=>a+b,0)/vals.length:null,min:vals.length?Math.min(...vals):null,max:vals.length?Math.max(...vals):null,module}}
export async function GET(req:NextRequest){const module=req.nextUrl.searchParams.get("module")||"points_eau",format=req.nextUrl.searchParams.get("format")||"csv",periode=req.nextUrl.searchParams.get("periode")||"personnalisee";const cfg=CFG[module]||CFG.points_eau;let q:any=supabaseAdmin.from(cfg.table).select("*").limit(20000);const start=periodStart(periode);if(start)q=q.gte(cfg.date,start);const{data,error}=await q;if(error)return NextResponse.json({ok:false,error:error.message},{status:500});const raw=data||[],rows=clean(raw,cfg.cols),s=summary(raw,module),title=`PSORE — Rapport ${module}`;
 if(format==="csv")return new Response(csv(rows),{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename="psore_${module}_${periode}.csv"`}});
 if(format==="xlsx"){const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet([{Indicateur:"Enregistrements",Valeur:s.enregistrements},{Indicateur:"Stations/ouvrages distincts",Valeur:s.stations},{Indicateur:"Moyenne",Valeur:s.moyenne??"—"},{Indicateur:"Minimum",Valeur:s.min??"—"},{Indicateur:"Maximum",Valeur:s.max??"—"}]),"Synthèse");XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows.length?rows:[{Message:"Aucune donnée"}]),"Détail des données");return new Response(XLSX.write(wb,{type:"array",bookType:"xlsx"}),{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","Content-Disposition":`attachment; filename="psore_${module}_${periode}.xlsx"`}})}
 if(format==="docx"){const headers=rows.length?Object.keys(rows[0]):cfg.cols.map((x:any)=>x[1]);const tableRows=[new TableRow({children:headers.map((h:string)=>new TableCell({children:[new Paragraph({text:h})]}))}),...rows.slice(0,5000).map(r=>new TableRow({children:headers.map(h=>new TableCell({children:[new Paragraph(String(r[h]??""))]}))}))];const doc=new Document({sections:[{children:[new Paragraph({text:title,heading:HeadingLevel.TITLE}),new Paragraph(`Période : ${periode} · ${s.enregistrements} enregistrement(s) · ${s.stations} station(s)/ouvrage(s) distinct(s)`),new Paragraph(`Moyenne : ${s.moyenne?.toFixed(2)??"—"} · Minimum : ${s.min??"—"} · Maximum : ${s.max??"—"}`),new Paragraph({text:"Détail des enregistrements",heading:HeadingLevel.HEADING_1}),new Table({width:{size:100,type:WidthType.PERCENTAGE},rows:tableRows})]}]});const b=await Packer.toBuffer(doc);return new Response(new Uint8Array(b),{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.wordprocessingml.document","Content-Disposition":`attachment; filename="psore_${module}_${periode}.docx"`}})}
 if(format==="pdf"){const pdf=new jsPDF({orientation:"landscape"});pdf.setFontSize(16);pdf.text(title,12,14);pdf.setFontSize(9);pdf.text(`Période: ${periode} | Enregistrements: ${s.enregistrements} | Stations/ouvrages: ${s.stations} | Moy: ${s.moyenne?.toFixed(2)??"—"} | Min: ${s.min??"—"} | Max: ${s.max??"—"}`,12,22);const hs=rows.length?Object.keys(rows[0]):[];let y=30;const widths=hs.map(()=>270/Math.max(hs.length,1));function head(){pdf.setFontSize(6);hs.forEach((h,i)=>pdf.text(String(h).slice(0,24),12+widths.slice(0,i).reduce((a,b)=>a+b,0),y));y+=5;pdf.line(12,y-3,285,y-3)}head();for(const r of rows){if(y>195){pdf.addPage();y=14;head()}hs.forEach((h,i)=>pdf.text(String(r[h]??"").replace(/\s+/g," ").slice(0,32),12+widths.slice(0,i).reduce((a,b)=>a+b,0),y));y+=4}return new Response(pdf.output("arraybuffer"),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="psore_${module}_${periode}.pdf"`}})}return NextResponse.json({ok:false,error:"Format non supporté"},{status:400})}
